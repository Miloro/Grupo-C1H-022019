language: java
dist: trusty
sudo: required
cache: 
  directories:
    - $HOME/.m2
jdk:
  - oraclejdk8
script: 
  - cd desapp-groupc1h-backend 
  - mvn clean install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8
